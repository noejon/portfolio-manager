{
  "name": "portfolio-manager",
  "version": "0.0.1",
  "description": "A helper tool to manage and re-balance a share portfolio",
  "main": "src/index.ts",
  "scripts": {
    "build": "rm -rf ./build && tsc",
    "dev": "nodemon",
    "prod": "yarn build && node build/index.js",
    "test": "jest"
  },
  "keywords": [],
  "author": "Jonathan Noé",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.0"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/node": "^14.14.12",
    "nodemon": "^2.0.6",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.2"
  }
}
